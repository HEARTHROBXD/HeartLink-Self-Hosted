HEARTLINK_DATABASE_NAME=heartlink
HEARTLINK_DATABASE_USER=heartlink
HEARTLINK_DATABASE_PASSWORD=replace-with-a-long-random-password
HEARTLINK_DATABASE_ROOT_PASSWORD=replace-with-a-different-root-password
HEARTLINK_REGISTRATION_ENABLED=true
HEARTLINK_PUBLISH_IP=0.0.0.0
HEARTLINK_PANEL_PUBLISH_IP=0.0.0.0
HEARTLINK_HANDSHAKE_KEY_PATH=./secrets/cloud-handshake.key
HEARTLINK_HANDSHAKE_KEY_ID=self-hosted-cloud-v1

# Optional account/unlock recovery delivery. Configure at least one webhook.
# HEARTLINK_RECOVERY_EMAIL_WEBHOOK=https://notify.example.com/heartlink/email
# HEARTLINK_RECOVERY_SMS_WEBHOOK=https://notify.example.com/heartlink/sms
# HEARTLINK_RECOVERY_WEBHOOK_TOKEN=replace-with-provider-bearer-token
# HEARTLINK_RECOVERY_PEPPER=replace-with-at-least-32-random-characters
TZ=Asia/Shanghai

# Only compose.1panel.yaml uses these:
# HEARTLINK_DATABASE_HOST=mysql-container-name
# HEARTLINK_DATABASE_PORT=3306
# HEARTLINK_DOCKER_NETWORK=1panel-network-name
