# Contributing

By contributing, you agree that changes are licensed under the license of the component they modify. Discuss security-sensitive architecture before implementation, add tests with behavior changes, keep protocol changes backward-compatible or versioned, and run `scripts/check.ps1`. Never commit real hosts, credentials, private keys, access tokens, or production ciphertext.

